/**
 * Global application constants
 * Centralizes hardcoded values to ensure consistency and easy updates.
 */

// Commerce Settings
export const COMMERCE_CONFIG = {
  SHIPPING_COST: 10.00,
  TAX_RATE: 0.00, // Currently 0% tax
  DEFAULT_COUNTRY: "USA",
  DEFAULT_CURRENCY: "$"
};

// Pagination
export const PAGINATION = {
  DEFAULT_PAGE_SIZE: 12,
  ADMIN_PAGE_SIZE: 20
};

// UI/UX
export const UI = {
  TOAST_DURATION: 4000,
  ANIMATION_DELAY: 0.1
};
